function findDigits(n) {
    // Write your code here
    // let res = 0;
    // let arr = n.toString().split('');

    // arr.forEach(el => {
    //     if(Number.isInteger( Number(el) / n) && Number(el) !== 0){
    //         res++
    //     };
    // });
    return n;
};
let res = findDigits('Wooow');

console.log(res);